email {
    disable_email = <%= disable_email %>;
    email_program = <%= email_program %>;
    email_name = "<%= email_name %>";
    email_address = "<%= email_address %>";
    email_number = <%= email_number %>;
    email_duration = <%= email_duration %>;
};
